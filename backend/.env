# -----------------------
# Server
# -----------------------
NODE_ENV=development
PORT=3001

# อนุญาต origin ของเว็บฟรอนต์ (ไม่มี / ท้าย และคั่นด้วยคอมมา)
FRONTEND_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://127.0.0.1:5500

# สำหรับลิงก์ในอีเมลลูกค้า/แอดมิน (อย่าใส่ / ท้าย)
FRONTEND_BASE_URL=http://localhost:5173
ADMIN_BASE_URL=http://localhost:5173/admin
# สำหรับ loopback เรียก API ภายใน (confirm-payment → inventory/sale)
API_BASE_URL=http://localhost:3001

# -----------------------
# JWT / Auth
# -----------------------
JWT_SECRET=changeme-super-secret-long-and-random
JWT_EXPIRES=7d

# -----------------------
# PostgreSQL
# -----------------------
PGHOST=localhost
PGPORT=5433
PGUSER=postgres
PGPASSWORD=6710210461
PGDATABASE=project19
PGSSL=false

# -----------------------
# Debug (ตัวเลือก)
# -----------------------
DEBUG_SQL=1

# -----------------------
# Order Status Mapping (ให้ตรงกับตาราง order_statuses)
# -----------------------
ORDER_STATUS_DRAFT_ID=pending
ORDER_STATUS_PAID_ID=o1

# -----------------------
# Mailer / SMTP (Gmail + App Password)
# -----------------------

# === เลือกหนึ่งแบบเท่านั้น ===
# [A] TLS 465 (แนะนำ)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true

# [B] STARTTLS 587
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false

SMTP_USER=prachmaejo@gmail.com
SMTP_PASS=<GMAIL_APP_PASSWORD_16_CHARS>

MAIL_FROM="Prach Maejo <prachmaejo@gmail.com>"
ADMIN_EMAILS=aprachmaejo@gmail.com,prachmaejo@gmail.com

# เปิด debug ตอนทดสอบ (ขึ้น production ให้เปลี่ยนเป็น false)
SMTP_DEBUG=true
